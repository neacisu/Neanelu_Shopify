# ============================================
# Docker Compose Environment Variables Template
# ============================================
# INSTRUCȚIUNI:
# 1. Copiază acest fișier: cp .env.compose.example .env.compose
# 2. Completează valorile (sau folosește direct .env pentru toate)
# 3. .env.compose NU SE COMITE (este în .gitignore)
#
# NOTĂ: Docker Compose va citi automat din .env dacă există

# ============================================
# PostgreSQL 18.1 (OBLIGATORIU)
# ============================================
POSTGRES_USER=shopify
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=neanelu_shopify_dev

# ============================================
# pgAdmin 4 (doar pentru dev)
# ============================================
PGADMIN_EMAIL=admin@local.dev
PGADMIN_PASSWORD=your_pgadmin_password_here
